{
  "entities": {
    "Interaction": {
      "title": "Interaction",
      "description": "A user-isolated journal entry or multi-turn reflection interaction with Gemini",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the interaction document",
          "maxLength": 128
        },
        "userId": {
          "type": "string",
          "description": "UID of the owner user matching auth.uid",
          "maxLength": 128
        },
        "title": {
          "type": "string",
          "description": "Title or topic of the reflection entry",
          "maxLength": 200
        },
        "mode": {
          "type": "string",
          "description": "Reflection mode: deep_reflection, brainstorm, summary, inquiry",
          "maxLength": 64
        },
        "prompt": {
          "type": "string",
          "description": "User journal entry or starting prompt text",
          "maxLength": 10000
        },
        "response": {
          "type": "string",
          "description": "Gemini AI reflection response and guidance",
          "maxLength": 20000
        },
        "summary": {
          "type": "string",
          "description": "Concise key insight or bullet summary generated by Gemini",
          "maxLength": 2000
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Creation timestamp"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Last update timestamp"
        }
      },
      "required": ["id", "userId", "prompt", "response"]
    },
    "UserProfile": {
      "title": "UserProfile",
      "description": "User profile metadata strictly isolated to the owner",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "UID of the user",
          "maxLength": 128
        },
        "email": {
          "type": "string",
          "description": "User email address",
          "maxLength": 256
        },
        "displayName": {
          "type": "string",
          "description": "Display name",
          "maxLength": 256
        },
        "lastLoginAt": {
          "type": "string",
          "format": "date-time",
          "description": "Last login timestamp"
        }
      },
      "required": ["userId"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "User profile document owned strictly by the authenticated user"
    },
    "/users/{userId}/interactions/{interactionId}": {
      "schema": "Interaction",
      "description": "User-isolated reflections, journal entries, and multi-turn conversations with Gemini"
    }
  }
}
